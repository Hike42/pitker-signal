# Netlify detects Next.js and installs its maintained OpenNext adapter.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "24"
  NETLIFY_NEXT_SKEW_PROTECTION = "true"
  NEXT_TELEMETRY_DISABLED = "1"
